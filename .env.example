# Copy to .env (gitignored) and fill in. Load with: uv run --env-file .env python -m risklens.live_pipeline
# Optional backup price source, used only when Yahoo fails. Free key: tiingo.com > Account > API
TIINGO_API_KEY=
